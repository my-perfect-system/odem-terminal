---
argument_specs:
  main:
    short_description: Install and configure Python tooling
    description:
      - Installs Python system packages and deploys per-user Python venv.
    options:
      users_catalog:
        type: dict
        required: false
        description: User identity catalog keyed by unique id
      users_list:
        type: list
        required: false
        description: Per-host user assignments referencing catalog
